{
  "name": "ares",
  "version": "0.6-beta",
  "description": "Apex Research Exploration System (ARES)",
  "author": "Frank DeFalco",
  "private": true,
  "workspaces": [
    "./frontend",
    "./backend",
    "./docs"
  ],
  "scripts": {
    "serve": "concurrently \"npm run serve --workspace=frontend\" \"npm run start --workspace=backend\"",
    "build": "npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
